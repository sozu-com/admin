import { AclPermissionDirective } from './acl-permission.directive';

describe('AclPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new AclPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
