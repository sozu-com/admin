import { OnlyDecimalDirective } from './only-decimal.directive';

describe('OnlyDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
