import { UptoTwoDigitDecimalNumberDirective } from './upto-two-digit-decimal-number.directive';

describe('UptoTwoDigitDecimalNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new UptoTwoDigitDecimalNumberDirective();
    expect(directive).toBeTruthy();
  });
});
