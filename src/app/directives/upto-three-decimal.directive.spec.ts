import { UptoThreeDecimalDirective } from './upto-three-decimal.directive';

describe('UptoThreeDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new UptoThreeDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
